{
  "scripts": {
    "lernaclean": "lerna clean --yes",
    "lernabootstrap": "lerna bootstrap --registry=https://registry.npm.taobao.org/ --loglevel=verbose",
    "rebootstrap": "npm run lernaclean; npm run lernabootstrap",
    "cleanall": "lerna run clean; lerna exec -- rm -rf ./dist ./yarn*; npm run lernaclean",
    "rebootall": "npm run cleanall; npm run lernabootstrap",
    "autod": "autod"
  },
  "devDependencies": {
    "autod": "^3.0.1",
    "cfx.require-plugin-coffee": "^0.2.10"
  },
  "dependencies": {
    "lerna": "~2.11.0"
  }
}
